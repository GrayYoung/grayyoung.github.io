/**
 * Default for the pages that don't need modules else.
 */

/* The following comment tell gulp-jshint variable define is require in another file. */
/* global require */
require([ './config' ], function(config) {
	require([ 'app/controller/global' ]);
	locatorMap
		
AIzaSyBECsgJK9qsTirYdxvaPWfOywXxiwdYe5k
	require({
		urlArgs : ''
	}, [ 'https://maps.googleapis.com/maps/api/js?key=' + mapBrowserAPI_Key + '&callback=initMap&language=en' ]);
});